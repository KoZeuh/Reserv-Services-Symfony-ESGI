<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container px-4 px-lg-5">
        <a class="navbar-brand" href="{{ path('home') }}">{{app_name}}</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                <li class="nav-item"><a class="nav-link active" aria-current="page" href="{{ path('home') }}">Accueil</a></li>
                {% if app.user %}<li class="nav-item"><a class="nav-link" href="{{path('booking_my_list')}}">Mes réservations</a></li> {% endif %}
            </ul>

            {% if not app.user %}
                <a href="{{ path('login') }}" class="btn btn-outline-dark d-flex align-items-center">
                    <i class="bi bi-box-arrow-in-right me-1"></i> Se connecter
                </a>

                <a href="{{ path('register') }}" class="btn btn-outline-dark ms-3 d-flex align-items-center">
                    <i class="bi bi-person-plus-fill me-1"></i> S'inscrire
                </a>
            {% else %}
                <a href="{{ path('logout') }}" class="btn btn-outline-dark d-flex align-items-center">
                    <i class="bi bi-box-arrow-right me-1"></i> Se déconnecter
                </a>
            {% endif %}

        </div>
    </div>
</nav>